# Instagram Accounts with Popular Reels

This script retrieves a list of Instagram accounts that have at least one popular reel based on a user-defined criteria. A popular reel is determined by the number of views.

## Prerequisites

Before running the script, you need to obtain an Instagram access token. Follow the steps below to set up the necessary prerequisites:

1. **Create an Instagram App**: Visit the Instagram Developer Platform (https://developers.facebook.com/docs/instagram/) and create a new app to obtain an access token.

2. **Install Python**: If Python is not already installed on your system, download and install Python from the official Python website (https://www.python.org/downloads/).

## Installation

Follow the steps below to install and set up the script:

1. **Clone the Repository**: Clone or download the repository to your local machine.

2. **Install Required Packages**: Open a terminal or command prompt, navigate to the script's directory, and run the following command to install the necessary package:


## Configuration

Configure the script by setting your Instagram access token:

1. **Set the Access Token**: Locate the `instagram_script.py` file in the downloaded repository and open it in a text editor.

2. **Replace the Access Token**: Find the line that says `access_token = 'YOUR_INSTAGRAM_ACCESS_TOKEN'` and replace `'YOUR_INSTAGRAM_ACCESS_TOKEN'` with the access token you obtained from the Instagram Developer Platform.

## Usage

To run the script and retrieve the list of Instagram accounts with popular reels, follow these steps:

1. **Run the Script**: Open a terminal or command prompt, navigate to the directory where the script is located, and run the following command:


2. **Adjusting Criteria**: By default, a popular reel is considered to have at least 10,000 views. If you want to change this criterion, open the script file in a text editor and modify the `min_views` variable to your desired value.

## Output

After running the script, it will display a list of Instagram accounts with popular reels based on the specified criteria.

## Error Handling

If you encounter any errors or issues during the installation or execution of the script, please ensure that you have followed the steps correctly and have a stable internet connection. For further assistance, you can reach out to [your contact information or support resources].

## Limitations

Please note that the Instagram API imposes certain rate limits and restrictions. Ensure that you comply with Instagram's policies and guidelines while using the API.

## License

[Specify the license under which the script is distributed, if applicable.]

## Contact Information

For any questions or support regarding this script, feel free to reach out to [your contact information].

